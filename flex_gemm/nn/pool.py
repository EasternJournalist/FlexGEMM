import torch
import torch.nn as nn
import torch.nn.functional as F


class SparseAvgPool(nn.Module):
    # TODO
    def __init__(self, kernel_size: tuple[int, ...], stride: tuple[int, ...], padding: tuple[int, ...]):
        super().__init__()        


class SparseMaxPool(nn.Module):
    # TODO
    def __init__(self, kernel_size: tuple[int, ...], stride: tuple[int, ...], padding: tuple[int, ...]):
        super().__init__()
        